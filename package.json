{
  "name": "quant-decision-engine",
  "private": true,
  "description": "Root package for Vercel — actual Next.js app lives in /frontend",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend"
  }
}
