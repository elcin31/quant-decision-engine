# Quant Decision Engine — Frontend

Next.js App Router + TypeScript client for the quantitative research engine.

```bash
cp .env.example .env.local
npm install
npm run dev
```

Requires the FastAPI backend on `NEXT_PUBLIC_API_URL` (default `http://localhost:8000`).
